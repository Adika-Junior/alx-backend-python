apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
  labels:
    app: messaging-app
spec:
  type: ClusterIP
  selector:
    app: messaging-app
    version: blue
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
---
# Service for blue version (can be used for gradual traffic switching)
apiVersion: v1
kind: Service
metadata:
  name: messaging-app-blue-service
  labels:
    app: messaging-app
    version: blue
spec:
  type: ClusterIP
  selector:
    app: messaging-app
    version: blue
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
---
# Service for green version (can be used for gradual traffic switching)
apiVersion: v1
kind: Service
metadata:
  name: messaging-app-green-service
  labels:
    app: messaging-app
    version: green
spec:
  type: ClusterIP
  selector:
    app: messaging-app
    version: green
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000

